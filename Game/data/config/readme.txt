config parameters:
ShowCamera, 1 or 0; bool //shows camera sprite
ShowDonationAlerts, 1 or 0; bool
ShowDonationAlertsType, separate, inCamera; string
ShowTwitchAlerts, 1 or 0; bool
ShowTwitchAlertsType, separate; string
ShowGame, 1 or 0; bool
DonationAlertsSocketRate, uint; // receive rate of donation alerts socket in miliseconds
TwitchReceiveSocketRate, uint; // receive rate of twitch socket in miliseconds
TwitchSendSocketRate, uint; send rate of twitch socket in miliseconds
TwitchSendBufferSize, uint;
TwitchRecvBufferSize, uint;
